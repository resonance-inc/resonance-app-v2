import { ResetPasswordError } from "@/src/components/auth/reset-password-error";

export default function ResetPasswordErrorPage() {
  return (
    <div className="block">
      <ResetPasswordError />
    </div>
  );
}
